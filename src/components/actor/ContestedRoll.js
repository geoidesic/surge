import { isNumber } from "../../helpers/Utility";
export default class ContestedRoll {
  #params = void 0;

  constructor(params) {
    this.#params = params;
  }

  weapon(item, actor) {
    //- get array of targets
    //- get map of target intents
    //- get contest codes from intents
    //- offer any dialog options to targets
    //- once all options are gathered, roll for both parties
    //- output to chat
    //- apply effects to target
  }

}
